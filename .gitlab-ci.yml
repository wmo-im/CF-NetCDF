build_docs:
	init:
		- apt-get update -y
		- apt-get install -y pandoc
		- gem install kramdown
	compile:
		- pandoc ./markdown/*.md -t docx -o ./doc/volume_I4.docx
		- kramdown ./markdown/*.md --output pdf > ./pdf/volume_I4.pdf
	artifacts:
		untracked: true
	only:
		- master
